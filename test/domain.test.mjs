import{expect,test}from'vitest';import{store,GovernanceError}from'../domain.mjs';const bad=(c,f)=>{try{f();return false}catch(e){return e instanceof GovernanceError&&e.code===c}};test('governs supplier API schemas',()=>{const d=store();d.propose('api-architect','a',{id:'API-293',name:'Supplier Contract',sdl:'type Supplier { id: ID! status: String! }'});expect(bad('conflict',()=>d.publish('api-release-manager','m','API-293'))).toBe(true);d.review('api-reviewer','r','API-293','Independent contract review confirms compatibility and governance controls.');expect(d.publish('api-release-manager','m','API-293').state).toBe('published');expect(bad('validation',()=>d.propose('api-architect','a',{id:'x',name:'x',sdl:'x'}))).toBe(true);expect(bad('conflict',()=>d.propose('api-architect','a',{id:'API-293',name:'Duplicate',sdl:'type Supplier { id: ID! status: String! }'}))).toBe(true);expect(bad('forbidden',()=>d.review('api-architect','a','API-293','Independent contract review confirms compatibility and governance controls.'))).toBe(true);expect(bad('not_found',()=>d.review('api-reviewer','r','API-X','Independent contract review confirms compatibility and governance controls.'))).toBe(true)});
